# Shared Packages

Current shared packages:
- `apps/backend/packages/*` (auth, api-client, hooks, ui, utils, types, config)
- `sdk/` for language SDKs

This root folder is reserved for a future workspace move if needed.
