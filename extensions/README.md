# Extensions Domain Map

Extension workspaces reserved by the monorepo:
- `merchant-assistant`
- `growth-automation-ai`

Existing merchant assistant logic already exists in `apps/backend/src/modules/merchant-assistant` and `apps/backend/src/extensions/merchant-assistant`.
