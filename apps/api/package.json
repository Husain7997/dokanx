{
  "name": "@dokanx/api",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "description": "Canonical API workspace that maps onto the running backend implementation in apps/backend.",
  "scripts": {
    "dev": "npm --prefix ../backend run dev",
    "start": "npm --prefix ../backend run start",
    "test": "npm --prefix ../backend run test",
    "test:unit": "npm --prefix ../backend run test:unit"
  }
}
