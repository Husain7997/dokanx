import { ScreenPlaceholder } from "./screen-placeholder";

export function AuthScreen() {
  return (
    <ScreenPlaceholder
      title="Mobile Auth"
      description="Authentication flow entry point scaffolded for sign-in, token bootstrap, and session hydration."
    />
  );
}
