import { PagePlaceholder } from "@dokanx/ui";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Developer account settings route scaffolded for keys, webhooks, and team access."
    />
  );
}
